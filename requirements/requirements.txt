# requirements.txt

# Flask for the local web server
Flask>=2.3.0

# Watchdog for monitoring live log file changes
watchdog>=3.0.0

# Optional: colorama for colored terminal output (not required but helpful)
colorama>=0.4.6
